apiVersion: v1
kind: Service
metadata:
  name: portfolio-green
  labels:
    app: portfolio-green
  namespace: portfolio

spec:
  ports:
  - port: 80
    targetPort: 80

  selector:
    app: portfolio-green

  type: ClusterIP
