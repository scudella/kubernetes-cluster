apiVersion: v1
kind: Service
metadata:
  name: portfolio-green
  labels:
    app: portfolio-green
  namespace: portfolio

spec:
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30102

  selector:
    app: portfolio-green

  type: NodePort
