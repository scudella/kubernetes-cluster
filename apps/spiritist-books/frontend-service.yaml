apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: spiritist-books
  labels:
    app: spiritist-books
    component: frontend
      
spec:
  selector:
    app: spiritist-books
    component: frontend

  ports:
  - port: 80
    targetPort: 80

  type: ClusterIP
