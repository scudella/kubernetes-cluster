# Merge this option into the existing Fedora kubelet configuration.
#
# Persistent node address:
KUBELET_EXTRA_ARGS=--node-ip=10.100.0.2
#
# If /var/lib/kubelet/kubeadm-flags.env is used instead, append:
# --node-ip=10.100.0.2
#
# Do not replace the existing kubeadm-generated arguments wholesale.
